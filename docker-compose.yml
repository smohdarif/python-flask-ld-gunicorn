version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LAUNCHDARKLY_SDK_KEY=${LAUNCHDARKLY_SDK_KEY}
      - LD_FLAG_KEY=${LD_FLAG_KEY:-sample-flag}
    env_file:
      - .env 